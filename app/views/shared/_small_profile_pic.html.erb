

<% if current_user.avatar_content_type == nil %>
	<%= image_tag avatar_url(params[:id]), class: "img-circle avatar-small" %>&nbsp;
<% else %>
	<%= image_tag(current_user.avatar.url(:thumb), class: "img-circle avatar-small") %>&nbsp;
<% end%>