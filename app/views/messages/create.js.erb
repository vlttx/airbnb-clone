<% publish_to conversation_messages_path(@conversation) do %>

		$('#chat').prepend("<%= j render @message %>");
<% end %>
	$('#new_message')[0].reset();
// by default the form's id in index.html is new_message
// the [0] is referring to the text area

